{
  "name": "aptronixchat-root",
  "version": "1.0.0",
  "description": "Root package.json for static CDN deployment fallback",
  "scripts": {
    "build": "cp -r client/* .",
    "start": "node server/server.js"
  }
}
